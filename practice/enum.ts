
// enum is set of related value, as set of descriptive constant

enum options {
    READY,          // 0 order 0 value
    NOT_READY       // 1 order 1 value
}

console.log(options.NOT_READY)      // default value one for first value